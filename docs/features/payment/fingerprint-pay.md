# 指纹支付

> 使用指纹快捷确认支付。基于 TEE 的安全加密方案, 支付密码经过加密存储

## 类别

红包与支付

## 类型

可开关 & 可点击

## 描述

启用后可在微信支付时使用指纹代替输入支付密码, 快速完成支付确认。

本功能采用基于 TEE (可信执行环境) 的安全加密方案, 支付密码在设备的安全区域内经过加密存储, 不会以明文形式暴露。指纹识别调用系统级生物识别 API, 支付密码本身不会泄露给第三方。

## 使用方法

需设备支持指纹识别。在模块设置中启用后, 点击, 设置支付密码以启用指纹支付功能
