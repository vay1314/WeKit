# 崩溃拦截 (Native)

> 拦截 Native 层崩溃并记录详细信息, 支持查看和导出日志

## 类别

调试

## 类型

可开关

## 描述

拦截微信 Native 层 (C/C++ 代码) 的崩溃信号 (如 SIGSEGV、SIGABRT 等), 记录崩溃时的寄存器和堆栈信息。捕获到的崩溃日志可在界面中查看, 并支持导出分享, 便于定位 Native 层问题。

该功能需要配合对应架构的 Native 库支持, 在检测到崩溃信号时会自动保存 tombstone 格式的日志文件。

## 使用方法

在模块设置中启用
