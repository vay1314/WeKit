# DPI 修改

> 自定义微信屏幕密度

## 类别

界面美化, 系统与隐私

## 类型

可开关 & 可点击

## 描述

自定义微信使用的屏幕密度 (DPI) 值, 通过修改 `DisplayMetrics` 来改变微信界面的显示缩放比例。数值越大, 界面元素越小, 可显示更多内容; 数值越小, 界面元素越大。

默认值为 360。建议设置合理的 DPI 值, 过大或过小可能导致界面显示异常。

同时会调整底部 Tab 图标的缩放比例, 使其在新 DPI 下保持合适的显示大小。

## 使用方法

在模块设置中启用后点击输入所需的显示宽度值
